monitors:
  - name: "Monitor with on-match action"
    network: "ethereum"
    filter_script: |
      tx.value > ether(10)
    notifiers:
      - "Monitor with on-match action"
    on_match:
      - "Test action"
